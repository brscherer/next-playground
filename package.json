{
  "name": "next-playground",
  "private": true,
  "scripts": {
    "dev:server-components": "pnpm --filter @np/server-components dev",
    "build:all": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  }
}